# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a Claude Code skills repository.

## Architecture

### Skill Structure

Each skill has a `SKILL.md` with YAML frontmatter (`name`, `description`) defining the interactive process Claude follows.


## Commit Convention
- Do **not** add `Co-Authored-By` or any AI/Claude contribution trailers to commit messages.
